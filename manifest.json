{
  "manifest_version": 3,
  "name": "Rainbow Recorder — Full Event Capture",
  "version": "0.10.0",
  "description": "브라우저 상호작용 전량+최종값 수집, 메뉴/라우팅/상태 힌트(page_view/route_change/post_state) 포함, 세션·환경·네트워크 매핑, 확장된 AZ_* 컬럼으로 CSV 내보내기",
  "permissions": [
    "storage",
    "webNavigation",
    "webRequest",
    "downloads",
    "tabs",
    "alarms"
  ],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_start",
      "all_frames": true
    }
  ],
  "action": { "default_title": "Export CSV (AZ Extended)" },
  "commands": {
    "export-csv": {
      "suggested_key": { "default": "Ctrl+Shift+E", "mac": "Command+Shift+E" },
      "description": "Export az_db_rows CSV"
    }
  ]
}
